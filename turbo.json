{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "globalPassThroughEnv": [
    "BETTER_AUTH_URL",
    "BETTER_AUTH_SECRET",
    "COOKIE_PREFIX",
    "CORS_ORIGIN",
    "DATABASE_URL",
    "DIRECT_URL",
    "RESEND_API_KEY"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "postinstall": {
      "cache": false
    }
  }
}
